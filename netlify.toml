[build]
  command = "echo Deploy estatico INVCRIPTO IA - usando dist precompilado"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
